import 'package:flutter/material.dart';
import 'home_screen.dart';

class wate extends StatelessWidget {
  const wate({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: home_screen(),
    );
  }
}